#include "../utils.h"

#define MIN_OBJLEN 3
#define MAX_OBJLEN 5

void set_init(struct rhifunc func);
void set_reserve(struct rhifunc func);
void set_insert(struct rhifunc func);

void map_init(struct rhifunc func);
void map_reserve(struct rhifunc func);
void map_insert(struct rhifunc func);
